{
  "name": "@vbobel/design-system",
  "private": true,
  "scripts": {
    "build": "lerna exec --scope=@vbobel/design-system-ui-styles -- npm run build",
    "prerelease": "npm run build",
    "release": "lerna publish --conventional-commits --dist-tag latest"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
